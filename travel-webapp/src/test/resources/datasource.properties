#dataSource.driverClassName=${db.driverClassName}
#dataSource.url=${db.url}
#dataSource.username=${db.username}
#dataSource.password=${db.password}
#hibernate.hbm2ddl.auto=${db.hbm2ddl.auto}
#hibernate.dialect=${db.dialect}
#hibernate.show_sql=${db.showSQL}
#hibernate.connection.pool_size=${db.connectionPoolSize}

dataSource.driverClassName=org.hsqldb.jdbcDriver
dataSource.url=jdbc:hsqldb:mem:set-repository
dataSource.username=sa
dataSource.password=
hibernate.hbm2ddl.auto=create-drop
hibernate.dialect=org.hibernate.dialect.HSQLDialect
hibernate.show_sql=true